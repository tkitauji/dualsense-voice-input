# DualSense実機テスト

このチェックはDualSenseをUSB接続したWindows 11 PCで行ってください。Bluetooth接続では、環境によってコントローラー内蔵マイクがWindowsの録音デバイスとして公開されません。

## 前提

1. DualSenseをUSBケーブルでPCへ接続する。
2. Windowsの「設定 → システム → サウンド → 入力」に `マイク (Wireless Controller)`、`Microphone (Wireless Controller)`、または `DualSense` を含むデバイスが表示されることを確認する。
3. 「設定 → プライバシーとセキュリティ → マイク」で、マイクアクセスとデスクトップアプリのアクセスを許可する。

## テスト項目

- [ ] アプリを起動すると、入力デバイス欄でDualSenseマイクが自動選択される。
- [ ] 「モデルを準備」を押すと約142MBのモデルを取得し、「準備完了」と表示される。
- [ ] テキストエディターへカーソルを置き、`Ctrl + Shift + Space`を押すと録音が始まる。
- [ ] DualSenseへ向かって「今日は音声入力のテストです」と話す。
- [ ] もう一度`Ctrl + Shift + Space`を押すと録音が止まり、認識結果がテキストエディターへ貼り付けられる。
- [ ] アプリ内の入力結果にも同じ文章が表示される。
- [ ] 「自動貼り付け」をオフにすると、結果は他アプリへ貼り付けられない。
- [ ] 「コピー」で結果をクリップボードへコピーできる。
- [ ] 録音中にDualSenseを抜いても、アプリが終了せずエラー表示へ戻る。
- [ ] 再接続して「再読込」を押すとDualSenseが再表示される。

## 既知の制約

- 管理者権限で動くアプリへは、通常権限の本アプリから自動貼り付けできません。
- CPUはAVX、AVX2、FMA、F16Cに対応している必要があります。
- グローバルショートカットを別アプリが占有している場合は登録できません。
