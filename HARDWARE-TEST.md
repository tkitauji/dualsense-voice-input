# DualSense実機テスト

このチェックはDualSenseをBluetooth接続したWindows 11 PCで行います。USB接続のWindows録音デバイス経路も回帰テストできます。

## 前提

1. DualSenseをBluetoothでPCへ接続し、ライトバーが点灯していることを確認する。
2. Steam、DS4Windows、DSXなど、コントローラーへ出力するアプリを終了する。
3. USB/WASAPI経路もテストする場合は、「設定 → プライバシーとセキュリティ → マイク」でデスクトップアプリのアクセスを許可する。

## テスト項目

- [ ] アプリを起動すると、`Bluetoothマイク（直接）` が入力デバイス欄で自動選択される。
- [ ] 「モデルを準備」を押すと約142MBのモデルを取得し、「準備完了」と表示される。
- [ ] テキストエディターへカーソルを置き、`Ctrl + Shift + Space`を押すと録音が始まる。
- [ ] DualSenseへ向かって「今日は音声入力のテストです」と話す。
- [ ] もう一度`Ctrl + Shift + Space`を押すと録音が止まり、認識結果がテキストエディターへ貼り付けられる。
- [ ] アプリ内の入力結果にも同じ文章が表示される。
- [ ] 「自動貼り付け」をオフにすると、結果は他アプリへ貼り付けられない。
- [ ] 「コピー」で結果をクリップボードへコピーできる。
- [ ] 録音中にDualSenseを切断しても、アプリが終了せずエラー表示へ戻る。
- [ ] 再接続して「再読込」を押すとDualSenseが再表示される。
- [ ] 録音停止後、次の録音開始までマイク音声報告が停止する。

## 既知の制約

- 管理者権限で動くアプリへは、通常権限の本アプリから自動貼り付けできません。
- CPUはAVX、AVX2、FMA、F16Cに対応している必要があります。
- グローバルショートカットを別アプリが占有している場合は登録できません。
- Bluetooth直接入力はWindowsの通常の録音デバイス一覧には現れません。本アプリがDualSenseのHID音声を直接復号します。

